# Boomtown Athletics — Design System &amp; Decisions

**Last updated:** 2026-08-18
**Version:** 0.32.0 *(one v0.31.0 change reverted at owner clarification)*
**Prompt version:** 2.0.0

## Source of truth
`design.md` is canonical for **design** decisions. The **GitHub repo** is canonical for **code**.
On context doubt: re-read this file, re-ground, then proceed.

## HTML Preview
- Local: open any `.html` at the repo root directly in a browser — there is no build step.
- Deployed (**GitHub Pages**, fronted by Cloudflare DNS/CDN): https://www.boomtownathletics.com
- `[CORRECTION] v0.28.0` — this block previously read "Local: `site/index.html`" (no `site/`
  directory has ever existed in this repo) and "Deployed (Cloudflare Pages): _TBD on first
  push_" (it is GitHub Pages, and it has been live for months).

## Brand tokens (derived from 2026 logos)
| Token | Hex | Use |
|---|---|---|
| `--ink` | `#0a0a0b` | Page surface |
| `--ink-2` | `#141417` | Raised panels |
| `--gold` | `#eec51d` | Boomtown gold (primary accent) |
| `--gold-2` | `#f6c733` | Hover / Queens Club gold |
| `--navy` | `#07085b` | Queens Club only |
| `--bone` | `#f4f2ec` | Body text on dark |

**Type:** Display = **Anton** (condensed, tournament-poster). Body = **Archivo**. Utility/data = **JetBrains Mono** ("scoreboard" times, eyebrows, buttons).
**Signature:** gold "serve-line" rules + mono scoreboard type for hours/times. Nocturnal gym tone (open 5–11PM).
**Logos:** `assets/img/boom-logo.png` (transparent, 2026 primary "BOOM"), `assets/img/qc-logo.png` (transparent Queens Club).

## Pages
Nav: **Home · Women's · Men's · Co-Ed · Training · Tournaments · Drop-In · Contact**. Partners = Home section. Queens Club hidden.

| Page | Status | Schema | Data |
|---|---|---|---|
| Home `/` | ✅ built | SportsActivityLocation | IG feed (KV) |
| Women's League `/womens-league` ⭐ | pending | FAQPage + Event | Sheet: Leagues |
| Men's League `/mens-league` | pending | FAQPage + Event | Sheet: Leagues |
| Co-Ed Leagues `/co-ed-leagues` | pending | FAQPage | MPS (featured) + Volo (rec) |
| Training `/training` | pending | FAQPage | static (adult + youth) |
| Tournaments `/tournaments` | pending | FAQPage + Event | Sheet: Tournaments |
| Drop-In `/drop-in` | pending | FAQPage | static |
| Contact `/contact` | pending | — | static |
| Queens Club `/queens-club` | ✅ built | none · `noindex` · robots-disallow · sitemap-excluded | → Women's League |

## Data layer (Google Sheet → published CSV per tab)
- **Leagues:** `title, division(womens|mens), format(2s|4s|6s|indoor), date, time, location, link, status`
- **Tournaments:** `title, date, location, link, status`
- **Partners:** `name, logo_url, website, tier(sport|league|gear|community)`
- Women's `2s`/`4s` rows may carry a Queens Club `link` (hidden until QC goes live).

## Partners (Home, tiered)
Sport/governing: USAV RMR, AVP · Leagues: Match Point Social, Volo · Gear: lululemon, Rhone, adidas · Community: Colorado Athletics Foundation, Aurora Public Schools, Special Olympics CO, Texas Roadhouse.
Removed (→ future FieldhouseUSA site): Meet.Play.Chill, Hoop Dream Nation, REAL Futsal, Zara Gymnastics, Rise Up.
_Logos: currently rendered as wordmark chips; official logo files to be dropped into `assets/img/partners/` or the Partners tab `logo_url`._

## Instagram (locked)
Behold free JSON → Cloudflare **Worker on Cron (every 12h)** → write to **KV** (+ R2 media re-host for the free >6-post workaround) → site reads `/api/ig`. Origin ≈ 60 hits/mo vs Behold's 1,200 free cap. Use Behold-hosted (`behold.pictures`) / R2 URLs, never raw IG CDN (expires). **v2 path documented separately** (Instagram API w/ Instagram Login, `instagram_business_basic`, 60-day token refresh, token in Worker Secrets).

## Hours (JSON-LD)
Mon–Fri 17:00–23:00 · Sat 19:00–23:00 · Sun 16:00–23:00.

## SEO
No Google Business Profile (deliberate — avoids reviews/griefing). Funnel = Instagram + word of mouth + competitive-intent search. `address` = Aurora; `areaServed` = Denver metro. Per page: unique title/meta/H1, OG, FAQPage; Event JSON-LD for leagues/tournaments from the Sheet.

## DATA LAYER (merged from design v0.14.0 — canonical here as of v0.15.0)

## 3. THE DATA LAYER (new — build this)

### 3.1 One spreadsheet, two tabs
Google Sheet ID currently in use: `11gSs2akMUBsRrR-80HPU8n7HJz5v-oT6HfGmsnFkn-w`
(the existing sheet is human-formatted and mixed; a cleaned two-tab version was produced this
session — see `Boomtown_Tournaments_Leagues_clean_v1_2026-06-28.xlsx`).

**Tab A — `Events`** (drives the tournaments/leagues/events board). Columns (header row exact,
lowercase):
```
type | start_date | end_date | title | time | location | registration_link | division | format | status | notes
```
- `type` ∈ `tournament` | `league` | `event`  ← the filter column
- `start_date`,`end_date` = **YYYY-MM-DD** (machine-readable; end blank for single-day)
- `status` ∈ `upcoming` | `past` | `open (rolling)` | `open (recurring)` | `date TBD`
- `division` (womens/mens/coed/junior/open), `format` (2s/4s/6s/doubles/grass/sand/short court/indoor)
- `registration_link` = full https URL (or blank; "Invitational" → leave blank, note it)

**Tab B — `Partners`** (logos, not dated events — deliberately separate):
```
name | tier | website | logo_url
```
- `tier` ∈ `sport` | `leagues` | `facility` | `community` | `gear`
  → rendered labels: Sport & governing · Co-Ed league partners · Facility partners · Community · Gear
- `logo_url` = hosted image (prefer `assets/img/partners/*` on the repo); blank → wordmark tile.

> Partners are a separate tab (not a `type` in Events) because they are not date-based and need a
> logo layout, not a schedule card. Both tabs live in the **same spreadsheet** and are surfaced by
> the **same filter bar**.

### 3.2 Publish to web (owner, one time)
Sheet → **File → Publish to the web → select the tab → Comma-separated values (.csv) → Publish.**
Do it for **Events** and **Partners**. Gives two CSV URLs the site fetches. (Verified current path,
2026.) Only publish these two tabs; keep anything private off them (published = publicly readable).

### 3.3 The widget (built this session — reference implementation)
File: `boomtown-events-widget_v2_2026-06-28.html` (self-contained; previews from built-in sample
data until the two URLs are pasted in). Behavior:

- **Filter bar:** `All · Tournaments · Leagues · Events · Partners` (segmented pills). `All` = all
  Events tab rows (date-sorted). `Partners` switches to tier-grouped logo tiles.
- **Auto-append:** renderer loops every CSV row → one tile per row. Add a row → a tile appears; no
  code change. Remove/finish a row → tile disappears.
- **Sorting:** Events sorted by `start_date` ascending (ISO strings sort chronologically). Undated
  rows collect under a "Dates to be announced" subhead at the bottom. Past events auto-hidden
  (`lastDate < today`), except league rows whose `status` contains `open` (always shown).
- **Register button:** rendered only when a valid `https` link exists; else "Invite only"
  (if status/notes say invitational) or "Details soon".
- **States:** loading skeleton; fetch failure → falls back to last-known/sample list with a quiet
  "updating, refresh shortly" note; empty → on-brand "Nothing on the calendar yet — check back soon."
- **Security:** all sheet text inserted via `textContent` (no `innerHTML` on data) → XSS-safe;
  links validated `^https?://` (blocks `javascript:`).
- **Motion (Emil):** custom `--ease-out: cubic-bezier(.23,1,.32,1)`; cards/tiles enter opacity
  0→1 + translateY(8→0) at 260ms, **staggered 40ms** via transitions (interruptible, not keyframes);
  buttons `:active { scale(.97) }`; hover gated behind `(hover:hover) and (pointer:fine)`;
  `prefers-reduced-motion` keeps the opacity fade, drops movement.
- **A11y:** real focusable `<a>` buttons with aria-labels; `aria-pressed` on filters;
  `aria-live="polite"` list region; gold/ink contrast AA.

### 3.4 Where it goes
Paste the `<style>`, the `.btw` section, and the `<script>` into the relevant page(s). Options:
(a) one combined "What's On" board (home or a `/schedule` page), or (b) drop the same block on
`tournaments.html` / league pages with the filter defaulted/limited to that type. Fonts already
load site-wide, so omit the font `<link>` when pasting.

---

## 4. Cutover checklist
1. Owner publishes Events + Partners tabs → sends two CSV URLs.
2. Paste URLs into `EVENTS_CSV_URL` / `PARTNERS_CSV_URL`.
3. Confirm live column headers match §3.1 exactly (lowercase).
4. Paste widget block into the target page(s); remove duplicate font links.
5. Verify: add a test row → appears after cache window (~a few min); bad/blank link → no button;
   past date → hidden; Partners tab → logo tiles by tier.
6. Commit once. Done — future edits are sheet-only.

## 5. Optional next builds
- **"Add an event" Google Form** → appends to the Events tab (friendliest entry incl. link field).
- **Per-page default filter** (e.g., tournaments page loads with `Tournaments` active).
- Migrate the cleaned xlsx into the live Google Sheet as the two canonical tabs.

## 6. Open flags (carried + new)
- Cache delay on published CSV is a few minutes (Google doesn't publish an exact TTL) — not instant.
- Some `location` values are "BTFH" vs "Boomtown Fieldhouse"; normalize in-sheet or in-render
  (widget already maps BTFH → "Boomtown Fieldhouse (Aurora)" in the cleaned data).
- Rows flagged for owner review in the cleaned file: "Boomtown Showdown 2026" dated 2025
  (title/year mismatch); two "Social Den" social events (keep on site?); date-TBD rows.
- Leagues with a past `start_date` but ongoing play: consider an explicit "active yes/no" instead of
  date-based past/upcoming.

## 7. If SEO Event rich-results become a priority later
Keep the exact same sheet. Add a scheduled GitHub Action that pulls the CSV and bakes rows +
`Event` JSON-LD into static HTML on a cron. This reintroduces a build step (which the owner
currently declined) but is the reliable path to indexed events. Design already supports it —
the sheet schema in §3.1 is sufficient to emit `Event` structured data.


## Change Log
- **Post-release check (2026-08-17)** — No code change. State re-verified against the live site and the live sheet ten days after 0.29.0 shipped, recorded here so the next session inherits facts rather than assumptions. **Confirmed live:** PRs #1 and #2 both merged 2026-08-07 16:05 UTC; `https://www.boomtownathletics.com/` serves `v0.29.0` on line 1; the announcement modal markup and all five registration links are present in the served HTML; `womens-league.html`'s hero CTA reads `League Registration` → `#schedule`. **`[FACT]` Design flaw found in 0.29.0, not previously recorded: the popup's items expire but its headline does not.** `<h2 id="antitle">` and `.ansub` are static text announcing *"4s Season Starts August 11"* and *"Two weeks of mixers … then pre-season 4s"*, while the five `.anitem` blocks self-retire on their `data-until` dates. The mismatch surfaces on a schedule: Kings Club Mixer disappears 2026-08-19, Queens Club Mixer 2026-08-20, Kings Club Pre-Season 2026-08-26, Queens Club Qualifier 2026-08-27 — **from 2026-08-27 the modal shows the Showdown alone beneath a headline advertising mixers that started sixteen days earlier.** The auto-expiry was designed so the modal could not outlive its content; it does not cover the copy *wrapping* that content, which is the same class of error one layer up. Recorded as a dated action item in `README.md` rather than fixed unasked, since the headline copy was owner-approved. `[INFERENCE]` The durable fix is to derive the headline from the earliest surviving item rather than hardcode it, but that trades a two-minute edit for logic that also needs maintaining — not obviously worth it for a seasonal promo. **`[FACT]` Sheet drift observed since 0.29.0.** Four Events rows gained `end_date` values between 2026-08-07 and 2026-08-17, all written as prose rather than ISO; three are chronologically impossible — Sally's Fundraiser RevCo 4s (`start_date` 2026-09-19) now ends "Monday, August 31, 2026", the Boomtown Showdown (`start_date` 2026-09-27) ends "Tuesday, September 1, 2026", and the Mile High Classic carries a valid-but-prose "Sunday, August 30, 2026". **Nothing renders wrong**, because the 0.22.1 guard ignores any non-ISO `end_date` — which is precisely why it went unnoticed for ten days and is worth recording: a silent guard converts a data-entry error into invisible debt, and the first change that starts trusting the column will print nonsense. The `end_date` column is drifting from a field into a scratchpad. **Unchanged and still open:** the Queens Club Pre-Season Qualifier row still reads `start_date` Tue 2026-8-25 / `day` "Tues & Wed" / `end_date` "Sunday, August 23, 2026" — the contradiction raised on 2026-08-07 and first flagged in `HANDOFF v0.28.0` §4b; `Qualifer` remains misspelled in the sheet though correct on the site; and `womens-league.html`'s second CTA (`Create your player profile`) still targets the old player form, left deliberately since only the hero button was in scope. **New content available:** Sally's Fundraiser RevCo 4s (2026-09-19) has gained a registration link and an 8 AM–2 PM time — a candidate for the modal when the mixers drop off. **`[FACT]` Standing false positive, recorded so it is not re-litigated: automated design linters flag `.detail{color:#e6a92b;font-weight:600;text-shadow:0 0 14px rgba(230,169,43,.4)}` as a "coloured glow shadow" on every page that carries it — all 11.** It is the site's intentional gold-glow brand token, documented in `docs/DESIGN-SYSTEM` §1 ("Always paired with a soft glow"), and it long predates this tooling work — the last commit to touch it is `9ac9c29`. **Do not "fix" it.** Changing it is a brand change across 11 files with no visual-regression coverage, and `CLAUDE.md` §2 is explicit that colour must not drift as a side effect of another task. The same linters flag `tbShine` (the topbar shimmer, 11 pages), the two home-page carousels, and `lrsk` (the schedule table's loading shimmer — continuous motion is that element's entire purpose). All pre-existing, all deliberate, all already covered by the 11 `prefers-reduced-motion` blocks and pause-on-interaction added in 0.22.0. If the carousels' auto-advance is ever revisited it should be a deliberate conversion decision, not a lint fix.
- **0.32.0 (2026-08-18)** — Single-item revert. Files changed: `index.html`, `README.md`, `design.md`. **[CORRECTION] The v0.31.0 entry's item (6) is reverted.** That release removed the Volo `.ptile` from the home partner wall on the instruction “Don't mention volo on the front.” The owner has since clarified the instruction was misread, and asked for the tile back beside Match Point Social. It is restored. The v0.31.0 entry above is left intact per this file's append-only rule — read it together with this line. **Restored from git, not retyped.** The markup was recovered verbatim from `ec32287:index.html` (the commit before its removal) rather than re-authored, because the tile carries an `onerror` text-fallback handler with nested quoting that is easy to get subtly wrong by hand. Verified by extracting the whole `#partners` section from both `ec32287` and the working tree and diffing them: **10,210 bytes each, byte-for-byte identical**, which proves position and attribute order as well as presence — a mere `grep -c volo` would have passed even if the tile had landed in the wrong tier. A second assertion parses the *Co-Ed league partners* tier and requires its captions to read exactly `["Match Point Social", "Volo"]`, in that order. **Nothing else from v0.31.0 is touched** — re-confirmed after the edit: the hero still carries two solid `.btn` CTAs plus the `.btn-ghost gold` Drop-In button, tile order is still Tournaments → Leagues → Drop-In, the Tournaments crop is still `50% 25%`, and the Leagues photo still has no inline `object-position`. `git diff` against v0.31.0 is **2 insertions, 2 deletions** — the line-1 comment and the partners line, nothing more. The `grep`-artifact rewording from the v0.31.0 follow-up commit (`e2ae2bd`, which took the literal token `noindex` out of `queens-club.html`'s version comment) also stands; this release's own comment deliberately says “co-ed league partner tile” rather than naming the brand, for the same reason. **Validation:** `bash scripts/validate.sh` → **79 passed, 0 failed**.
- **0.31.0 (2026-08-18)** — Owner revision pass on the v0.30.0 Leagues work, plus `queens-club.html` opened to search. Files changed: `index.html`, `schedule.html`, `queens-club.html`, `robots.txt`, `sitemap.xml`, `scripts/validate.sh`, `CLAUDE.md`, `README.md`, `design.md`. **(1) Hero CTA parity.** `Leagues` was `.btn-ghost`; the owner ruled leagues and tournaments equally important, so it is now `.btn` — verified in-browser to compute an identical `rgb(248,196,0)` background, border and `rgb(17,17,17)` text as `View tournaments`. Label changed `Leagues →` → `View leagues` for parallelism with its twin; flagged as a one-word revert if unwanted. **(2) Drop-In CTA gold outline.** New `.btn-ghost.gold{border-color:#F8C400}` appended to the existing `.btn-ghost` rule **in `index.html` only** — a one-declaration modifier, so it does not need the 11-page copy-paste treatment and cannot sit inert elsewhere. Verified: transparent background, `rgb(248,196,0)` border, white text; the two `.btn-ghost` Facility buttons further down the page still compute the original `rgba(255,255,255,.28)` border, confirming the modifier is scoped and did not leak. **(3) Queens Club opened to search.** `noindex, nofollow` removed; bare `<title>Queens Club</title>` replaced with a descriptive title; description, full OG set, Twitter card and `theme-color` added; `SportsActivityLocation` JSON-LD added (hours `19:15`–`21:15` Tue–Sun, transcribed from the page's own "7:15 – 9:15 PM" table rather than invented); `Disallow: /queens-club` + `/queens-club.html` removed from `robots.txt`; the URL added to `sitemap.xml` and that file's "intentionally excluded" comment deleted. **`[FACT]` The load-bearing find was the canonical.** It read `https://www.queensclubvb.com/` — a *different domain*, which `curl` shows returns `302` to `https://queensclubvb-com.l.ink/`, a parked placeholder. A cross-domain canonical tells Google to index that URL instead, so **removing `noindex` on its own would have achieved nothing**; the page is now self-canonical. No prior document mentioned this. **Meta Pixel deliberately not added**, so the page is the one indexable page without it: the owner asked for search metadata, not analytics, and adding a tracker unasked is not a silent default. `validate.sh` check 7 still expects exactly 11 and its message now names the exception. `queens-club` was promoted into the script's `$INDEXABLE` list, which is what raises the suite from 77 to **79 checks** — the page now gets its version header, tag balance, inline-JS parse and **JSON-LD parse** checked, and all four pass. **`[FACT]` Metadata is complete; content is not.** The body is ~25 words. It will not rank for a competitive query as-is, and "You have been invited / Tap the crest to enter" reads oddly to a stranger arriving from Google. Raised with the owner rather than rewritten — visible copy is a content decision. **(4) Both cine photos re-cropped, measured not guessed.** A browser probe reported, per tile, the exact **source-pixel band** made visible by `object-fit:cover` given the live box aspect and `object-position`. *Tournaments* (`womens-usav-nationals-champions.jpg`, 2400×1412) sat at `50% 42%`; at a 1536-wide box that band starts at source-y **328** while the tallest head is at ~**288** — the reported head clipping, reproduced numerically. Lowered to **`50% 25%`**, giving 132–166px of headroom from 1280 to 1600 and still clearing at 2560. *Leagues* (`womens-league-full-group.jpg`, 1800×1200) was returned to **true centre** (inline `object-position` removed) after testing `46%`: the nudge bought ultrawide headroom nobody needs but reintroduced a clipped `www.boomtownathletics.com` banner strip at 1280 — 36 source px, plainly visible in a screenshot. At true centre the whole group is in frame at 1280 with 126px of headroom and **0px** bottom clip. `[GUESS]` The landmark Y values (head tops ~288/~380, banner bottom ~270) were read off the images by eye, so treat the margins as ±20px. **Accepted limit, stated rather than hidden:** this photo is 3:2 inside a ~3:1 band, so at viewports beyond ~2800 CSS px "no banner sliver" and "heads safe" become mutually exclusive and the top of the back row grazes the edge. Optimised for 1280–2560, which covers every real desktop including the owner's (measured at 1536 CSS px). **(5) A filter hash now scrolls the board into view.** v0.30.0 shipped hash-preselection only, and the release note said so explicitly; the owner asked for the scroll. `HASH_PICKED_FILTER` records whether the hash matched `HASHMAP`, and `focusBoard()` calls `scrollIntoView` on `#whats-on` after `render()` on **all three** `init()` paths (no-URL, success, catch) — missing the catch path would have made the scroll vanish exactly when the CSV fails. `#whats-on` gains `scroll-margin-top:90px` because `.head` is `position:sticky`; measured landing puts the board top at **90px** against a **53px** header, with the filter bar and first card both in view. `behavior` honours `prefers-reduced-motion`. This necessarily applies to `#tournaments`, `#events` and `#partners` too — verified: `#tournaments` scrolls to the same 445px offset with 4 Tournament cards — which is consistent with the owner's own "both are equally important" ruling. **Debugging note worth keeping:** the first scroll test reported `scrollY:0` and `typeof focusBoard === "undefined"` while the edits were provably in the file. Cause was a **stale cached document** — `navigate_page` reused it, and the live DOM was missing the new `style` attribute too. `fetch(...,{cache:"no-store"})` governs the CSV, not the HTML. Re-test with `ignoreCache` before believing a no-op. **(6) No Volo on the home page.** The `.ptile` was removed from the *Co-Ed league partners* tier, which now holds Match Point Social alone. `volo.png` stays in `assets/` — `schedule.html` still renders it from the Partners CSV, and `library.html` references it twice; this was a home-page instruction, not an asset deletion. The word also appeared once more in `index.html`: inside the line-1 version comment describing the removal. Reworded to "one co-ed league partner tile" so `grep -ci volo index.html` returns **0**. Note the two probes disagreed and both were right — `grep` counted the comment, the browser's `documentElement.innerHTML` did not, because a comment before `<html>` is outside it. **(7) Tile order is now Tournaments → Leagues → Drop-In.** Leagues moved from 3rd to 2nd; to keep the left/right/left rhythm Leagues takes `.cine right` and **Drop-In reverts to plain `.cine`**. Reordering without moving the `right` class would have put two left-aligned tiles side by side. **Mobile re-verified after every one of the above:** at 390px `scrollWidth === innerWidth`, a full-tree sweep finds **0** elements past the viewport edge, all three hero buttons are 55px tall, and tile order and alignment read correctly. **Validation:** `bash scripts/validate.sh` → **79 passed, 0 failed**.
- **0.30.0 (2026-08-18)** — Leagues surfaced on the home page: a hero CTA and a cinematic tile. Files changed: `index.html`, `README.md`, `design.md`. **Hero CTA.** The hero carried two buttons (`View tournaments` → `schedule.html#tournaments`, `Drop-in: Thu–Mon →` → `drop-in.html`); a third, `Leagues →` → `schedule.html#leagues`, is inserted **between** them, styled `.btn-ghost` so the gold `.btn` remains the page's single primary hero action. **The `#leagues` hash is not decorative and it is not an anchor** — `schedule.html` line 302 defines `HASHMAP={…leagues:"league"…}` and line 303 reads `location.hash` at parse time into `current`, so the hash *preselects the Leagues filter* before first render and line 408 sets `aria-pressed="true"` on the matching `.btw__filter` button. `[FACT]` No `id="leagues"` (or `id="tournaments"`) element exists on `schedule.html`, so neither hash scrolls; the existing `View tournaments` button has always behaved this way and the new button matches it rather than introducing a second convention. **Verified against the live published CSV, not the fallback rows:** tapping the hero button at 390px lands on `schedule.html#leagues` with the Leagues chip pressed and **5 cards rendered, every one `kind: "League"`**, titles matching the sheet's Events tab verbatim (`Queens Club Mixer and Open Gym (2 Weeks)`, `Queens Club Sunday RP`, `Queens Club Pre-Season Qualifer`, `Kings Club Mixer and Open Gym (2 Weeks)`, `Kings Club Pre-Season`) — not the `SAMPLE_EVENTS` titles, which proves the real fetch path, and no `.btw__note` empty state. This was checked **before** building, because a CTA that lands on "Nothing on the calendar yet" is worse than no CTA: the Events tab was read through the Drive MCP and holds 6 `League` rows, 5 of them `open (rolling)`, which `eventVisible()` admits on the status string regardless of date. **Leagues cinematic tile.** `Tournaments` (`.cine`) and `Drop-In Volleyball` (`.cine right`) were the only two full-bleed tiles; a third, `Leagues` (`.cine`, left-aligned), is inserted after Drop-In and before the `Find your game` card band, preserving the left/right/left alternation. Reuses the existing `.cine`/`.in2`/`.lead2`/`.mrow`/`.cta2` structure exactly — **no new CSS rule, class, hue, radius or breakpoint was added**, which matters on this site because the stylesheet is physically copy-pasted into all 11 pages and any new rule would have to be applied 11 times or sit inert. CTAs: `See leagues` (`.btn`) → `schedule.html#leagues`, `How to qualify →` (`.btn-ghost`) → `womens-league.html` (confirmed to carry real qualification copy — 8 `qualif*` matches, not a dead promise; the same target and phrasing the `.cards` Leagues tile already uses). **Photo: `library/womens-league-full-group.jpg` (1800×1200) moves from library-only to placed**, closing one of the four `ASSET-LIBRARY §4c` unplaced team shots. Chosen over `community/group-home.jpg` and `action/dropin-team-wide.jpg` after viewing all three: it is the largest indoor group (~45 people on the Boomtown court), the sharpest, and — counter-intuitively — the **best** of the three on mobile, because at a portrait crop the wider 1800×811 candidate retains only ~42% of its width against this one's ~62%. `[FACT]` It is a women's-division photo on a tile headed `Leagues`; that is representative rather than exact — the live sheet's league rows are 4 women's, 2 men's, 1 co-ed — and it is flagged here rather than glossed. **`object-position` deliberately left at the `50% 50%` default.** It was first written as an inline `50% 45%` copying the Tournaments tile's `50% 42%`; measured in Chrome at 1440×900 that crop left a 6px sliver of the banner's `www.boomtownathletics.com` strip clipped along the top edge. At the default the sliver is gone and the full group sits in frame, so the inline style was **removed rather than retuned** — a redundant declaration that merely restates the default is a future reader's false lead. **Copy is sheet-derived, not invented:** `Queens & Kings Club 4s`, `Rolling registration` and `Boomtown Fieldhouse` are each true of all 5 currently-visible league rows. An earlier draft read `Co-Ed 6s with Volo`; **cut, because the only co-ed row is `status: past` and `eventVisible()` hides it** — the tile would have advertised a division the board it links to does not show. The `lead2` still says "women's, men's and co-ed", which is accurate as an *offering* (`co-ed-leagues.html` exists and the nav lists it) rather than as a claim about today's board. **Mobile — measured, not assumed.** At 390px and 320px: `document.documentElement.scrollWidth === innerWidth` (no horizontal overflow) and a full-tree sweep found **0 elements** crossing the viewport edge; both new links are 55px tall, clearing the 44px tap target; tile height 460px at 390px, 408px at 320px; zero console errors. The hero's three buttons **stack into three ragged-left rows** below ~430px — `.hero-cta` is `flex-wrap:wrap` and `.btn` is `white-space:nowrap`, and the two pre-existing buttons already measured 212px + 13px + 214px = 439px against a 350px track, so they were **already** stacking into two such rows. The third row continues an existing pattern rather than creating one; the hero has `max-height:none` below 560px so it grows and nothing clips. No `.btn` padding or `.hero-cta` override was introduced to force two-per-row, because `.btn` is duplicated across 11 pages and the cascade risk outweighs the cosmetic gain. **Not changed, flagged instead:** the nav's `Leagues ▾` parent still points at `queens-club.html`, which is `noindex` and excluded in `robots.txt`, while the `.cards` Leagues tile and the footer both point at `womens-league.html` — three different Leagues destinations on one page. Out of scope for this request. **`docs/ASSET-LIBRARY_v0.28.0` is now stale in exactly one row:** `womens-league-full-group.jpg` reads `— 🗂 library-only` and is listed in §4c as awaiting placement. Left unedited rather than half-corrected, since re-versioning that document cascades into `CLAUDE.md` §10, `README.md` and the handoff; recorded here as the open item. **Validation:** `bash scripts/validate.sh` → **77 passed, 0 failed** (baseline before the change was also 77/0, on Python 3.12.10 — so the green is a real green, not the environmental all-fail described in §3).
- **0.29.0 (2026-08-07)** — Home-page announcement modal + Queens Club gate removal. Files changed: `index.html`, `queens-club.html`, `README.md`, `design.md`. **Announcement modal on `index.html`.** Opens **once per day** per visitor (`localStorage` key `bt_announce_2026_fall`, whose value is the local calendar date it was last shown; owner chose daily over per-session) after a 700 ms delay, listing five registrations in owner-specified order: Queens Club Mixer and Queens Club Pre-Season Qualifier under *Women's 4s*, Kings Club Mixer and Kings Club Pre-Season under *Men's 4s*, USAV Boomtown Showdown under *Tournament*. **Every date, time, venue and `forms.gle` link was read from the live sheet via the Drive MCP**, not transcribed from a prior document — the WomensLeagues, MensLeagues and Events tabs were all read this session. **Per-item auto-expiry** is the load-bearing design decision: each `.anitem` carries `data-until`, the script hides items past that date, hides a `.angrp` heading once all of its items have expired, re-applies the `.first` class to whichever heading now leads (so the top border doesn't reappear), and **returns before opening at all if nothing is live** — the modal cannot outlive its content, which is the failure mode a hardcoded promo block always eventually hits. Dates are constructed from `new Date(+y,+m-1,+d)` rather than `new Date("2026-08-19")`, because the string form parses as UTC and rolls back a day in Denver — the same timezone trap the league renderer already documents. Styling: **true black** at owner request — `#000` card and `#000` rows on an `rgba(0,0,0,.94)` veil, separated by `rgba(255,255,255,.18)`/`.16` hairlines rather than by surface tint, which is the same pure-black treatment `queens-club.html` already uses. This deliberately departs from the site's usual `#121214`/`#1b1b1e` near-black greys so the overlay reads as an overlay, not another band. Everything else reuses existing tokens (`#F8C400` CTA on `#111`, `.ey` eyebrow, Anton heading, 6px radius, the existing 560px breakpoint); **no new hue, radius or breakpoint was introduced**, and the elevation shadow is neutral black, not a coloured glow. Accessibility: `role="dialog"`, `aria-modal`, `aria-labelledby`/`aria-describedby`, focus moved to the close button on open and returned on close, a Tab focus trap over the live focusable set (computed at keypress time, so hidden items are excluded), Escape and backdrop-click to close, `prefers-reduced-motion` honoured, and the whole `.anv` is `hidden` until opened so it cannot flash pre-script. `z-index:120`, above the site's previous ceiling of 60. **`womens-league.html` hero CTA retargeted.** The first hero button was `Player registration` → `https://forms.gle/1sfPSZhbVEifFrct6` in a new tab; it is now `League Registration` → `#schedule`, an in-page jump to the Schedule table where each league carries its own Register link (`scroll-margin-top:90px` already present, so the sticky header does not cover the heading — measured landing at 74px from the viewport top). `target`/`rel` dropped since it is no longer external. The second CTA lower on the page (`Create your player profile`, same form) was **left alone** — the owner asked only about the hero button; flagged rather than silently changed. **Stale copy corrected as a direct consequence of the gate removal:** the page's `What is Queens Club?` FAQ claimed *"entry is by invitation code"*, which stopped being true the moment the prompt was deleted. Fixed in **both** the visible `<details>` answer and the `FAQPage` JSON-LD, which must agree or Google flags the structured data as mismatched. `[FACT]` A site-wide grep confirmed this was the only surviving claim of its kind. **Verified in Chrome, not asserted:** opens on first visit with focus on the close button, recording the date; does not reopen on a same-day reload; **reopens unprompted when the clock advances to the next day with storage left intact**; with the clock moved to 2026-08-20 both mixers are hidden and three items remain; at 2026-08-26 the entire *Men's 4s* heading disappears with its two expired items; at 2026-10-01 the modal never opens and the session key is never written; no horizontal overflow and the CTA goes full-width below 560px; zero console errors on every path. **Queens Club invitation gate removed.** `[FACT]` **The gate never validated anything** — `enter()` accepted any non-empty string and redirected. It was an aesthetic ritual, not access control, so removing it exposes nothing that was previously protected. The `.veil`/`.modal`/`.gate`/`.msg`/`.shake` CSS, the modal markup and the entire inline script are gone; the crest reverts to the plain `<a href="womens-league.html">` it always was underneath, with `aria-haspopup`/`aria-controls` dropped. A comment records why, so it is not reconstructed as "safety" later — the same reasoning that removed `esc()` in 0.27.0. **If access control is ever genuinely required it must be server-side**; nothing in a static GitHub Pages repo can gate anything. Also purged the page's stale line-2 `v0.18.0` comment, comment-scoped rather than line-based because it shared its line with `<html lang="en">` — the documented trap. `queens-club.html` moves from `v0.19.0` (8 releases stale) to `v0.29.0`, closing part of the 0.28.0 §2d gap; `404.html` and `library.html` remain out of scope and unversioned/stale. **Open question raised, not silently resolved:** the Queens Club Pre-Season Qualifier row carries `day` = "Tues & Wed" and `start_date` = Tue 2026-8-25 but an `end_date` cell reading "Sunday, August 23, 2026" — two days before the start. The modal renders "Tue & Wed · From Aug 25" to match `start_date` and the existing league tables; if the Sunday is correct, both the sheet and the modal need changing. This is the same unresolved row flagged in `docs/HANDOFF_v0.28.0` §4b. **Validation:** `bash scripts/validate.sh` → 77 passed, 0 failed.
- **0.28.0 (2026-08-06)** — Tooling handoff to Claude Code + repo hygiene. **No rendered change on any page; not one of the 11 indexable pages was edited, so none had its line-1 version comment bumped** (house style bumps only edited files — advertising a change that doesn't exist in a page is the misleading-version problem 0.27.0 was cut to fix). **Added:** `CLAUDE.md` at repo root (auto-loaded operating instructions — invariants, frozen constants, the three bug classes that have reached production, the verify-don't-inherit rule); `scripts/validate.sh` (77 automated checks: version headers, tag balance, inline JS syntax via `node --check`, JSON-LD parse, renderer `textContent` safety, the `.ig-card` inset invariant, frozen constants, asset-path resolution, placeholder scan, `_isISO` date guard); `docs/DESIGN-SYSTEM_v0.28.0_2026-08-06.md` (tokens extracted from the code — the site has almost no CSS variables, so that document *is* the token system); `docs/ASSET-LIBRARY_v0.28.0_2026-08-06.md` (every image with dimensions and per-page usage); `docs/HANDOFF_v0.28.0_2026-08-06.md`; and three slash commands under `.claude/commands/`. Retires the `EXEC-PROMPT` format, which existed solely to brief a chat session with no repo access. **Repo hygiene — 7 files deleted, 1 archived**, every one re-verified unreferenced against the checkout immediately before removal rather than trusted from the standing list: 4 orphan partner logos (`molten.jpg`, `real-colorado.png`, `team-evo.png`, `usav.png`), the stale standalone `boomtown-events-widget_v3_2026-07-19.html`, and both superseded manifests (`assets/img/ASSET-MANIFEST.md`, `WIX-IMAGE-MANIFEST_2026-07-19.md`). The documented near-collision trap held: `molten.png` and `team-evo-black.png` are live on `index` and `schedule` and were confirmed still present after the deletion. The root `HANDOFF.md` was **moved, not deleted**, to `docs/HANDOFF_ARCHIVE_pre-v0.28.0.md` — a stale handoff at repo root is the single most likely artifact for a future session to mistake for current state, but deleting it is the one step that isn't cheap to undo. **[CORRECTION] The v0.28.0 tooling was authored on 2026-08-06 and never committed.** `main`'s last commit was 2026-07-29 (`"Add files via upload"`), and the repo carried no `CLAUDE.md`, no `scripts/`, no `docs/` and no `.claude/` until this commit. Every prior statement that the release had been "delivered" described a folder on the owner's machine. Sixth consecutive release in which an inherited claim proved false — and a reminder that "shipped" means *present on `main`*, not *written*. **[CORRECTION] The `boomtownvb.com` 301 is already live.** `CLAUDE.md` §9, the handoff §4d and `README.md` all listed it as deferred until the domain transfers. Measured 2026-08-06: `https://www.boomtownvb.com` returns `301 Moved Permanently` → `https://www.boomtownathletics.com/`. A finished job sat on the open-work list across four handoffs because nobody re-checked it. **[CORRECTION] `validate.sh` has an undeclared Python 3 dependency.** It shells out to `python3` at five points — tag balance, script extraction, JSON-LD, the `.ig-card` inset check and the link/asset resolver — with no `command -v` guard, unlike its `node` calls, which skip gracefully. On the owner's Windows machine (no Python; `python3` resolves to a Microsoft Store stub) the first run reported **40 passed, 37 failed**, every failure environmental and none real. After installing Python 3.12.10 and adding a `python3` shim ahead of the Store alias on PATH, the same checkout returned **77 passed, 0 failed**. Recorded in `CLAUDE.md` §3; recommended follow-up is to port the five Python blocks to `node`, which the script already requires, removing the dependency rather than documenting it. A validator that fails for environmental reasons teaches its user to ignore red output, which is worse than having no validator. **[CORRECTION] `docs/ASSET-LIBRARY` overstated the image count.** Its summary line read "81 image files"; the measured inventory was **80** before this release and is **76** after. The per-file tables were accurate — only the total above them was wrong; the 3 apparent extra rows are a deliberate cross-reference table in §4b, not duplicates. **Also corrected:** `design.md`'s own HTML Preview block, which pointed at a `site/` directory that has never existed and described deployment as "Cloudflare Pages — TBD on first push" when the site is served by GitHub Pages and has been live for months. **Validation:** `bash scripts/validate.sh` → **77 passed, 0 failed** against the post-hygiene tree.
- **0.27.0 (2026-07-28)** — Header/dead-code hygiene + record corrections. **No rendered change on any page.** **Stale version comments purged:** line 1 (`<!DOCTYPE html><!-- vX.Y.Z · DATE · summary -->`) is the single canonical version header per house style, but lines 2–4 of all 11 indexable pages still carried leftover comments from older releases — 18 in total, up to three per page on `womens-league`, `mens-league`, `co-ed-leagues`. They contradicted line 1 and are an active hazard: reading line 2 of `index.html` reports `v0.19.0` and leads straight to the false conclusion that `main` is six releases behind (this actually happened at the start of this session before proper verification). Removal was **comment-scoped, not line-based**, because on 8 of the 11 pages the last stale comment shares its line with `<html lang="en">` — a line delete would have destroyed the document. **Dead `esc()` removed from 3 files:** `index.html` held the full HTML escaper orphaned when 0.26.0 rebuilt `card()` with DOM properties; `womens-league.html` and `mens-league.html` held empty `function esc(){}` stubs (the 0.26.0 handoff only flagged the `index.html` one). Each verified uncalled — exactly one `esc(` occurrence per file, the definition itself — and both league renderers confirmed to build every cell via `textContent` with `innerHTML` used only for clearing and a static skeleton span, so no escaping behaviour was lost. Register links are additionally gated on `/^https?:\/\//i`, so a `javascript:` URL in the sheet cannot become an anchor. **[CORRECTION] The 'CSV is browser-verified only' constraint is narrower than documented.** The v0.25.0 and v0.26.0 briefs state that sheet data cannot be verified outside a browser because `docs.google.com` blocks automated fetchers via robots. That holds for a plain HTTP fetch, but **not** for the Google Drive MCP, which reads the file through the Drive API on the owner's OAuth — all 7 tabs were read directly this release and matched expectations (WomensLeagues 6 rows, MensLeagues 2, Events 10). Standing limit that remains: Drive reads the **source sheet**, not the published-CSV cache the browser fetches, so publish latency is still unobservable from tooling. **[CORRECTION] External partner-logo count was understated.** Prior docs said "~14 hotlinks on index + tournaments". Measured: **22** — `logo.clearbit.com` ×18, `static.wixstatic.com` ×4 — across **three** pages: `index.html` 16, `schedule.html` 3, `tournaments.html` 3. `schedule.html` was absent from every previous list, so a future self-host pass driven by the old note would have missed it. **[CORRECTION] `start_date` normalisation would be a regression, not a fix.** The v0.27.0 exec-prompt's cleanup list implied normalising `2026-8-25` → `2026-08-25`. The renderer's test is `/^\d{4}-\d{1,2}-\d{1,2}$/`, so single-digit months already pass; the cells are *text*, and retyping them invites Google Sheets to coerce them to date values, after which the published CSV emits `8/25/2026`, fails the ISO test, and `dateText()` prints the raw string into the Date column. Guidance recorded: do not retype date cells; if unavoidable, set the column to Plain text first. **Cloudflare Worker identified (open since 0.26.0 §5d).** `boomtown-api.vvisuth.workers.dev` read via the Cloudflare MCP: it is the backend of a **separate project**, a members platform served from `10xequity/btplatform`, self-reporting `v0.32.0` at `/api/health` — 139 API routes (signup, waivers, passkey/WebAuthn, member profiles, moderated messaging, push, ICS feeds, facility bookings, POS, marketing, admin dashboard, MRR), a D1 database, R2 buckets, Square payments with webhook signature verification, Brevo email. **It is not a proxy** and has no bearing on this site's Google or Behold constraints — the 0.26.0 `[SPECULATION]` to that effect is disproven. Owner decision: out of scope for `btvb`, revisit later. A second Worker, `cobo-ig-feed`, is a 20-line Behold JSON proxy CORS-locked to `coloradoboom.com` (partner facility), unrelated to this site. Also noted: the platform Worker references `boomtownvb.com`, which the owner owns and will 301 to `boomtownathletics.com` when the domain transfers — deferred. **Verified, no action:** `main` genuinely carries the 0.26.0 work — `.t` inset fix on all 11 pages, both league gids wired, `card()` free of `innerHTML`, Meta Pixel on all 11 indexable pages. The single surviving `innerHTML` in `index.html` is the carousel's internal static SVG icon constant, not feed data. Only four gids appear anywhere in the repo, confirming the sheet's `Leagues` and `Tournaments` tabs are legacy and consumed by nothing. **Delivered as owner-action docs (not repo files):** `SHEET-CLEANUP_v0.27.0_2026-07-28.md` (cell-level edit list derived from the live Drive read) and `repo-hygiene_v0.27.0_2026-07-28.sh` (the long-outstanding `git rm` list as a script with a read-only pre-flight that re-verifies zero references at run time and aborts on a dirty tree). **Validation:** all 11 pages — tag balance across 34 element types, `node --check` (v22.22.2) on every inline script, JSON-LD parse, placeholder scan, plus assertions that line 1 reads v0.27.0, no stale comment survives, no `esc(` remains, the 0.26.0 `.t` inset invariant is intact, and all four CSV gids plus the Behold feed URL are unchanged. All pass.
- **0.26.0 (2026-07-28)** — Instagram grid rendering fix + tile hardening; scoring default confirmed. **Root cause of the "yellow boxes":** a CSS cascade leak, not a feed or network failure. `.ig-card span` (caption) declares `inset:auto 0 0 0`; `.ig-card .t` (the `@boomtownvb` chip) has higher specificity and overrode `top`/`left` but never `right`/`bottom`, so `right:0;bottom:0` survived and stretched the chip from (9,9) to the tile's bottom-right corner — measured in-browser at **336×336 inside a 347×347 card (94% coverage)**, a solid `#F8C400` panel over the photo. Fixed with `inset:9px auto auto 9px` (verified: chip → 96×21, 1.7% of tile; caption stays bottom-anchored). **Second victim of the same leak:** `.ig-card .pl` (reel badge) also inherited `padding:24px 11px 10px`; under `box-sizing:border-box` that 34px padding floor exceeded `height:26px`, so the badge rendered **26×34** — an oval with an off-centre ▶. Fixed with `inset:9px 9px auto auto;padding:0` (verified: 26×26). **Scope decision:** the IG card CSS is duplicated across all 11 indexable pages even though only `index.html` has the grid markup; all 11 were patched (owner-chosen) so the defect cannot resurface via page-copy, accepting 11 version-header bumps for 10 inert rules. **Tile hardening:** added a per-image `onerror` → local committed photo, guarded by a `data-fb` attribute so a failing fallback cannot loop; previously only a whole-feed fallback existed, so one blocked image left a permanently empty tile. **Security:** `card()` no longer uses `innerHTML` — it interpolated the feed's `img` URL into a `src="..."` attribute unescaped, an injection vector if the feed were ever compromised; the card is now assembled with `createElement`/`textContent`. **Decision recorded:** tournament scoring default is **side-out** (open since 0.22.0) — confirmed by owner; no code change, `tournaments.html` copy was already correct. **Corrected prior record:** IG images are served from `behold.pictures`, not `hop.behold.pictures` as stated in the 0.19.0 entry and the v0.25.0 brief — the earlier claim was carried forward without re-verification (same class of error as the 0.24.0 `spike.jpg` false alarm). **Verification method:** diagnosed and proven in a real browser via the Chrome MCP — feed 200, all 9 image requests 200, images decoded (`naturalWidth>0`), then element geometry measured before/after injecting the candidate fix. Note: `naturalWidth>0` alone was initially mis-read as "the grid works" — it proves decode, not paint order; the owner's screenshot is what exposed the overlay. Deferred by owner: partner-logo self-host, 7 library team photos, low-res re-exports.
- **0.25.0 (2026-07-28)** — League schedules went live. Wired both `window.__LEAGUE_CSV__` hooks to the entire-document publish token (`/e/2PACX-…/pub`) with per-tab gids — WomensLeagues `gid=1645496886`, MensLeagues `gid=1824462476`, each with `single=true&output=csv`. This is the same published token already driving the Events/Partners board on `schedule.html` (confirmed the entire-doc publish selects a tab by gid; no per-tab re-publish needed). Renderer + v0.24.0 header schema guard unchanged; verified against live tab contents (WomensLeagues 6 rows, MensLeagues 2 rows; timezone-safe dates; `TBD`/`Invitiational` reg cells → "Coming soon"). **Instagram:** no code change — the home grid already targeted the Behold feed `feeds.behold.so/JgI7koDkWULorgLXnzkz`; it populates now that the owner allowlisted the site (apex + www) in Behold. Behold is fetched browser-side, so Cloudflare needs no change. **Corrected prior false alarm:** the v0.24.0 handoff's "`spike.jpg` missing/broken on 4 pages" was a grep substring artifact — a full asset sweep this cycle found 0 missing images; no page references `assets/img/action/spike.jpg`, and `assets/img/spike.jpg` exists. Deferred by owner: partner-logo self-host, placement of the 7 library team photos, low-res re-exports, scoring-default confirmation.
- **0.24.0 (2026-07-28)** — League schedule + tournaments-photo release. **Leagues:** removed the on-page "⚠ SAMPLE ROWS" flag/caption text from both league pages (owner request — the schedule now shows rows with no warning banner in either sample or live mode; only an invisible `console.warn` remains on fetch failure). **Added a header schema guard** to both renderers: after fetch, if the CSV header lacks `title`/`start_date` it silently falls back to the sample rows instead of rendering `undefined` cells — this is the guard the v0.23.0 handoff described but that was never actually on `main`; verified necessary because the previously-supplied women's URL (gid 454802271) resolves to the **Partners** tab, which would otherwise paint blank rows. Renderer tested against the live sheet's WomensLeagues (6 rows) + MensLeagues (2 rows) contents in a Node harness: dates timezone-safe (`2026-8-25`→"Tuesday, August 25, 2026"), stale prose in `end_date` ignored, non-URL reg cells → "Coming soon". **Tournaments (#15b):** decorative tournament photo (`tournament-spike-block-action.jpg`, 1800×1202) added beside the Schedule section using the existing `.split` 2-col grid (collapses to 1-col ≤840px; `width`/`height` set to avoid CLS). **Dedupe:** identified 4 unreferenced/duplicate partner files for `git rm` (`molten.jpg`, `real-colorado.png`, `team-evo.png`, `usav.png`). **Blocked this session (need owner input):** self-hosting the ~14 clearbit/wixstatic-hotlinked partner logos (sandbox egress denies both hosts — needs source PNGs); MensLeagues + WomensLeagues published-CSV URLs; Instagram feed capture; true-high-res originals for the 3 low-res files; scoring-default confirmation.
- **0.23.1 (2026-07-28)** — Photo-library metadata + tagging patch (cumulative on 0.23.0). Embedded IPTC/XMP/EXIF (title, caption, keywords, by-line, credit, source, copyright, Denver·Colorado location) into the 10 team photos + `womens-league-hero.jpg`; metadata-only (pixels/dimensions untouched); no personal names — keywords use publicly visible jersey/team text + category terms. `library.html` `DATA` gained an optional 6th element (`tags` array) rendered as chips and matched by the search box; backward-compatible (untagged entries render unchanged).
- **0.23.0 (2026-07-28)** — Owner media/content pass (8 pages) + first live CSV connection. Home tournaments cine → nationals-champions photo (upscaled 2400px, face crop); carousel controls → bare glass triangles; Fieldhouse carousel leads with exterior sign + courts-empty added. Women's League: Queens Club hero title block ("Denver's Elite Women's League"), ~30% zoomed-out hero crop, qualify section moved above programs with action photo + Schedule anchor button, program photos swapped + equalized cards, rules FAQ → tournaments#gendered-4s, live WomensLeagues CSV wired with schema guard, SAMPLE flag removed when live. Men's: beach section removed, Yam Time champs photo wired, rules link. Tournaments: gendered-4s anchor. Co-Ed: Match Point photo wired. Contact/Training hero recrops. Library: ground-truthed usage labels + 10-photo pending batch.
- **0.22.1 (2026-07-28)** — League schedule renderer updated to the sheet's new column schema: `day` replaces `Night`; date derived from `start_date`/`end_date` (old `date_display` removed). Browser-side full-date formatting ("Tuesday, August 25, 2026") with ISO range support (same-month "Aug 28–30, 2026") and timezone-safe local parsing (no Denver off-by-one). Schedule column header Night→Day on both league pages. Removed a dead inline `var L=[…]` table script. Sample rows refreshed to mirror the current sheet under the new field names. Patch on 0.22.0; renderer parses by header name and ignores the sheet's leading `type` column.
- **0.22.0 (2026-07-28)** — Meta Pixel activated site-wide; home carousels → accessible JS with glass prev/next; tournaments Formats & Rules + How to Enter moved above schedule (side-out scoring default, time-outs, forfeit, waiver, co-ed rule fixes); men's page overhaul + CSV schedule renderer; women's CSV schedule renderer; library deletion queue; drop-in/skill-levels/training small moves; sanctioning wording.
- **0.21.0 (2026-07-27)** — schedule "Come Play!" heading + day-of-week chip (reads day||night); home mobile hero min-height clamp; IG feed catch logging. (Logged retroactively — shipped in code, not previously recorded.)
- **0.20.0 (2026-07-27)** — photo swaps + carousel additions; library.html added; robots/sitemap updates. (Logged retroactively.)
- **0.19.0 (2026-07-19)** — Instagram feed wired live + mobile/SEO pass. **IG feed:** home grid now fetches the Behold feed (feeds.behold.so/JgI7koDkWULorgLXnzkz) client-side, rendering up to 9 posts from the Behold-hosted (hop.behold.pictures) square crops — stable URLs, never raw IG CDN. Loading skeleton while fetching; **graceful fallback** to the local static image set (+ quiet) on any fetch error or empty feed; video/Reel posts get a ▶ badge; captions from prunedCaption, alt text trimmed for a11y; width/height on tiles to avoid CLS; “Load more” pages 9 at a time then flips to “View all on Instagram.” **Architecture note:** direct client fetch chosen over the design.md §IG Worker→KV cache — matches the no-build philosophy and stays under Behold’s free 1,200-views/mo cap at current traffic; the Worker path remains the documented scale option (same feed URL drops in). **Mobile:** IG grid 3-col → 2-col under 560px; the two new nav dropdowns (Tournaments/Drop-In) inherit the existing static-submenu mobile pattern in the hamburger. **SEO:** skill-levels.html given BreadcrumbList JSON-LD (Home › Tournaments › Skill Levels) alongside its FAQPage; IG tiles are JS-injected (social proof, intentionally non-indexed) so no LCP/crawl impact. **Verify in browser:** live feed parse + QC shine mask.
- **0.18.0 (2026-07-19)** — Content + nav + gate pass. **Nav (site-wide, 11 pages):** Tournaments is now a dropdown (Live Schedule · Format &amp; Rules · Skill Levels); Drop-In is now a dropdown (Drop-In Nights · FAQ → drop-in.html#faq); Leagues→Women’s now routes to **queens-club.html** (the gate) instead of womens-league directly. **Tournaments Formats &amp; Rules populated** from the live facility rule sets (USAV 2025–27 base + Boomtown mods: Revco 4s women’s-net/men restrictions, Co-Ed &amp; Gendered 4s with per-division hand-set standards, Co-Ed/Gendered 6s no-girl-rule, 2s, and shared match/scoring). Rulebook links + Facility Rules chip moved to the **top** of the page (hero chips + section intro) rather than the bottom. USAV link updated 2023–25→2025–27 edition. **New page skill-levels.html** (canonical /skill-levels, in sitemap + footer): Open→Rec ladder + “how to choose” guide, owner-authored copy (mirrors the MPS partner Skill Levels page, grammar-corrected, org refs → Boomtown), FAQPage JSON-LD, unique title/meta/OG. Linked under the Tournaments dropdown. **Queens Club rebuilt** as the Women’s landing gate: pure-black elite treatment, all photos/carousel/bg removed, gold logo with an animated shine sweep (mask-clipped to the crest, reduced-motion gated), tap-crest → accessible code modal (focus-trap, ESC/backdrop close, any code passes) → womens-league.html#qualify; Exit ✗ button (top-right) → home. womens-league remains directly indexable/crawlable and in the sitemap — the gate only intercepts human nav clicks. **Women’s League:** second photo added to the Indoor program card to balance the previously hanging text (was only Grass carried an image). **Home:** removed the black seam between the mailing-list CTA band and the first cine (margin-block 12px→0 on .cine); tournaments cine focal point raised (object-position 50% 28%). **Molten:** existing baked-in gray/checkerboard keyed out, cropped to wordmark, 2× upscaled (stopgap in delivery; owner replacing with official asset).
- **0.18.0 (2026-07-19)** — Content + nav + gate session. **Nav (all 10 pages):** Tournaments and Drop-In converted to dropdowns — Tournaments ▾ (Live Schedule · Format & Rules · Skill Levels), Drop-In ▾ (Drop-In Nights · FAQ→drop-in.html#faq). Leagues ▾ trigger + "Women's" item now route to **queens-club.html** (the gate), not womens-league directly. **Tournaments Formats & Rules:** populated with real USAV 2025–27-based rule copy adapted from the facility rule set — Match/Scoring, Reverse Co-Ed 4s, Co-Ed & Gendered 4s (incl. hand-set standard by division), Co-Ed & Gendered 6s (no "girl rule"), Short-Court 2s. Rules links (USAV rulebook PDF, USAV interpretations, AVP, Boomtown facility rules) moved to the **top** of the section; Facility Rules chip added to the tournaments hero. **New page skill-levels.html** (canonical /skill-levels, in sitemap): Open/AA/A/BB/B/Rec ladder + "how to choose" — content copied verbatim from the Match Point Social skill-levels page per owner (owner-authored), grammar-corrected, org references reframed to Boomtown; FAQPage JSON-LD added; linked under Tournaments ▾. **Queens Club → Women's gate:** rebuilt black/elite — carousel + faded bg photo removed, pure #000, logo crest with animated gold shine sweep (mask-based, reduced-motion gated); code entry now a focus-trapped modal (any non-empty code passes) → womens-league.html#qualify; Exit ✕ button top-right → home. Women's nav path is now Women's → Queens Club → tap crest → (code) → womens-league. womens-league.html stays directly crawlable + in sitemap so search entry is unaffected. **Women's League:** second photo (womens-team-boom) added to the Indoor program card to balance the previously text-only left column. **Home:** black gap between mailing-list CTA band and first tournaments cine removed (main .cine margin-block 12px→0); tournaments cine focal point raised (object-position 50% 28%). **Molten:** existing molten.png cleaned (baked checkerboard/halo keyed out, cropped to wordmark, 2× upscaled) as a stopgap on the white tile; official asset to be dropped in by owner. **Fix:** missing </main> restored on 7 pages (tournaments, drop-in, training, womens-league, mens-league, co-ed-leagues, schedule) — pre-existing invalid markup. **Pending gates:** host (GH Pages vs Cloudflare Pages), www/apex + 301, Meta Pixel ID.
- **0.17.0 (2026-07-19)** — LIVE DATA WIRED. Owner published Events (gid 2097603747) + Partners (gid 454802271) CSVs; both URLs baked into schedule.html and the standalone widget file. Added gid-order auto-detect (if the Events feed arrives with `tier` instead of `type` columns, feeds swap automatically) — removes tab-order ambiguity since the CSVs could not be fetched for verification from the build sandbox (robots + egress allowlist). Widget still falls back to sample data + quiet note on fetch failure. Verification of live parse must happen in a browser (open schedule.html). Sheet file: Drive ID 1UFsYrtD1pf27f6D0m3O45oA3MUtfG2wiV9x1zyl-H4w (rebuilt 2026-07-19, 102 rows). Remaining gates: host decision, www/apex, Pixel ID, push to main, DNS.
- **0.16.0 (2026-07-19)** — What's On page shipped. New **schedule.html** (canonical /schedule, added to sitemap): tournaments.html shell + embedded widget v3 (anchored extraction; widget body rule stripped so host styles win; no font link). **Hash filters:** schedule.html#tournaments|#leagues|#events|#partners preselects the filter and syncs the buttons. **Link rewiring site-wide:** nav "Tournaments", home "View tournaments"/"Schedule →" and every tournaments.html#schedule CTA → schedule.html#tournaments. tournaments.html keeps Formats/Qualifier/Enter; stale hardcoded placeholder schedule table (fake Mar–May 2026 rows) + its dead JS removed → CTA to the live board. **All raw Google-Sheet links removed** from user-facing pages (v0.14.0 decision: never show the spreadsheet). **Perf fix:** v0.15.0 lazy pass had lazied 9 hero images — reverted to eager + fetchpriority="high" (LCP). CSV URLs pending — widget renders sample data until pasted into EVENTS_CSV_URL/PARTNERS_CSV_URL in schedule.html.
- **0.15.0 (2026-07-19)** — Audit + regeneration session. **SEO:** sitemap namespace typo fixed (sitemaps.org); lastmod added; all meta descriptions rewritten <=160 chars; og:image:width/height/alt on every indexable page; facility-rules given full OG + WebPage/BreadcrumbList JSON-LD; branded 404.html added; inactive Meta Pixel placeholder (YOUR_PIXEL_ID) on indexable pages. **Perf:** loading="lazy" + decoding="async" on below-the-fold images site-wide (nav logo + hero kept eager). **Widget v3:** JetBrains Mono removed entirely (site never loads it -> browser-default mono fallback caused the bad look); Anton for titles/day numeral, Archivo for all UI text. **Queens Club:** body::after gold radial wash removed; logo glow tightened to drop-shadow(0 3px 8px black) + drop-shadow(0 0 14px gold .16) — subtle rim, no bloom. **Data layer sections of v0.14.0 merged into this file (below); v0.14.0 doc retired.** **Verified:** Behold pricing (Starter $10/mo = 3 feeds/50 posts/hourly — covers both sites; free tier's 6-post cap is the real blocker for the 3x3 grid). **Pending gates:** host (GH Pages vs Cloudflare Pages) drives canonical strategy; CSV URLs; widget placement; Pixel ID; push to main.
- **0.13.0 (2026-06-21)** — Banner sizing, photo de-dup, video, QC, partner logos. **Banner (#1):** beneath-hero band switched from the large .sband to the slim .topbar sizing on all 9 pages (co-ed now matches siblings). **Photos (#2,#3):** co-ed MPS now a group photo (was a duplicate of the hero); women’s league swapped off the over-used team photo to a unique group shot; alignment object-position added. **Fieldhouse (#4):** facade re-cropped higher so the "Fieldhouse USA" sign is no longer clipped. **COBO (#5):** club block removed from home and embedded on the Training page (its rightful youth/club home); video processed with ffmpeg — Gemini watermark removed via delogo, downscaled 960x540, faststart, dark-bg poster, clean dark container (no white box). **Partner logos (#6):** USAV RMR reverted to the prior RMR mark on a white tile (no black bg); Team Evo recolored to black text; Molten white background removed (transparent PNG). **QC (#7):** added a faded women’s-group background, a 3-photo carousel, and a compact schedule table to fill dead space and add engagement. **CTA (#9):** gold mailing-list band added to the bottom of every sister page. **Mobile (#8):** reviewed — grids/breakpoints/stacking intact, video + QC additions responsive. **Limitation:** Texas Roadhouse / Town Center / Chance Sports / My Spark Denver still use runtime logo sources (clearbit) with text fallback — third-party logo files can’t be downloaded into the repo from the build sandbox; drop official PNGs into assets/img/partners/ to host locally.
- **0.12.0 (2026-06-21)** — Banner mirror + hero legibility. Removed the top gold banner on every page; the single band beneath each hero now carries the full "Denver, Colorado’s Official Volleyball Company · Sanctioned by USAV · AVP · AAU" copy (the redundant "OFFICIALLY SANCTIONED BY" band retired). Strengthened hero drop-shadow (h1/sub/eyebrow/p) site-wide so text reads cleanly over photos.
- **0.11.0 (2026-06-21)** — Dead-zone + motion pass. **Blank-area fixes:** replaced all in-body Wix-hosted content photos (co-ed x2, drop-in, training, womens) and the 9 Instagram thumbnails with local assets so nothing 404s when leaving Wix (these had no onerror fallback = hard blanks). **Animations (reduced-motion gated):** Fieldhouse spec numbers count up on scroll; hero serve-line draws in; partner tiles stagger-fade per tier on reveal; IG-card + spec hover states. **Audit:** section padding/grids/breakpoints reviewed — no margin or empty-container defects found; clearbit logos already degrade to wordmark text via onerror. JS syntax-validated.
- **0.10.0 (2026-06-21)** — Subpage + content pass. **Menu bug (#7):** dropdowns vanished crossing the 8px gap between trigger and panel — added an invisible hover-bridge (`.has-sub::after`) and snapped `.submenu` to `top:100%` on desktop; applied to all 9 pages. **About (#1,#10):** removed "player-run club" framing; rewrote from the original site copy (premier Denver-metro volleyball home at FieldhouseUSA); single photo → rotating carousel of group photos (closer crops, less dead space). **Drop-In (#2):** cine now uses a live-play action shot (dropin-play-wide) instead of the posed team. **Tournaments (#3):** spike crop shifted up (top-bias .30→.08) for more action/less floor. **Hero (#4):** subhead left-justified; drop-shadow added to hero h1/sub + cine headings across all pages for contrast. **Fieldhouse (#5):** background re-cropped to the building facade and overlay gradient cut ~50%. **Partners (#6,#11,#12):** reverted the serve-line background; Gear tier moved beneath Community; swapped to owner-supplied official logos (Molten/Volo/COBO on white tiles; USAV/adidas/Special Olympics CO on dark tiles for contrast); added Colorado Boom Volleyball Club video block denoted "Competitive Club Pathway · Our Partner" (autoplay/muted/loop, poster fallback). **Co-Ed (#9):** removed the top gold banner; moved its fuller copy ("Denver, Colorado’s Official Volleyball Company · Sanctioned by USAV·AVP·AAU") into the band beneath the hero. **SEO/mobile (#8, global):** menu/shadow fixes + contact JSON-LD added; OG already local from v0.9.0. **Removed:** low-res tournament-celebration.jpg. **Flags:** USAV tile uses the national USA Volleyball mark on the "USAV RMR" label; Real Colorado logo saved as asset but not placed (was a deliberately-removed partner).
- **0.9.0 (2026-06-21)** — Home image/SEO pass. **Cine fix (#1,#3):** Tournaments + Drop-In cinematic blocks were using near-square images (spike.jpg 819x868, team.jpg) under object-fit:cover → heavy zoom/crop; replaced with purpose-built 16:9 crops (tournaments-spike-wide, dropin-team-wide) and lowered .cine min-height to clamp(400,54vh,520). **CTAs (#2):** Tournaments cine now "Register now" → tournaments.html#schedule and "Formats" → #formats (was "See the schedule"/"How to enter#enter"). **About (#3):** swapped side-cropped banner for womens-team-boom.jpg (native 4:3, no one cropped). **Fieldhouse (#4,#5):** carousel rebuilt from real venue interior shots (court/lounge/bar) + one action frame, centered; section gets faded exterior-front photo as background (.fh-bg opacity .16 + heavy dark/gold gradient, text remains AA-contrast). **Partners (#6):** flat #121214 → #0e0e10 with gold radial glow + diagonal "serve-line" repeating stripes + tile gradient/gold hover ring (3 alt ideas documented for owner). **Assets (#7):** owner uploads renamed kebab-case into assets/img/venue + community/action; EXIF ImageDescription/Artist/Copyright embedded across 28 JPGs; ASSET-MANIFEST.md added. **SEO/mobile (#8):** added SportsActivityLocation JSON-LD to Home (was missing despite prior claim); repointed og:image from wixstatic → local og-cover.jpg (1200x630) on all 8 indexable pages; added twitter:image, og:url/type, theme-color; width/height on hero/cine imgs to cut CLS; mobile cine/carousel ratios. **Flag:** JSON-LD geo lat/long is approximate — verify. design.md previously claimed Home shipped SportsActivityLocation; it did not until now.
- **0.8.0 (2026-06-21)** — Logo/hero/photo refinement. **Tab icon (#1):** favicon + apple-touch regenerated from the hi-res 2026 master (`Boom_Logo_2026.jpg`); spiker isolated to gold only (no grey 'M' ghost), aspect-preserved square so it no longer looks smooshed; nav/hero/footer logo upgraded to the 1053px clean master. **Hero edge-to-edge (#3):** collage re-cropped to remove the bottom white band (2000x1120); home hero now uses a matched `aspect-ratio:2000/1120` (min 360 / max 720px) so `object-fit:cover` shows the whole grid edge-to-edge without zoom-crop on typical screens; bg opacity raised .55->.62 so the grid reads. **Photos (#2):** 9 new shots downscaled (1800w) into assets/img/action (grass-women, coed-four, youth-camp) and assets/img/community (group-court/home/net/banner1/banner2, four-banner); About photo -> community BOOMTOWN-banner group shot; Fieldhouse carousel now mixes play + community-in-venue frames; women's Grass card gets the outdoor grass-women photo. **Aligned heroes (#5):** Co-Ed -> co-ed doubles foursome; Contact -> community banner group. **Women's registry (#4):** CTAs relabeled to "Player registration" / "Create your player profile", pointed at the current women's form (forms.gle/1sfPSZhbVEifFrct6) — swap this URL for the new Google Form when the player-profile registry is built. **Note:** on ultra-wide/very-narrow screens the fixed-ratio hero still crops a little (min/max-height caps); a fixed-ratio image can't be full-width AND fully uncropped at every width.
- **0.7.0 (2026-06-21)** — 17-item review pass. **Global:** sanctioned band ("OFFICIALLY SANCTIONED BY · USAV · AVP · AAU") now on every page beneath the hero; favicon wired to gold spiker mark (was still boom-sm); nav/hero/footer logo swapped to clean transparent `boom-logo.png` (black bg + halo removed); nav reordered to Tournaments · Drop-In · Training▾(Adult/Youth) · Leagues▾ · Store · Contact, added Team Store (boomtownathletics.square.site) + Training dropdown; back-to-top button site-wide; motion system propagated to all sub-pages; descriptive alt on all hero/photo images. **Home:** hero now full action-collage matrix (only white bottom trimmed) centered (less zoom); cinematic Tournaments/Drop-In given vertical breathing room + taller; About photo → real team photo; FieldhouseUSA single image → animated crossfade carousel of interior action shots. **Sub-pages:** action-photo heroes throughout (8 new 24MP shots downscaled); Women's qualifying fully rewritten (pre-season Aug–Oct half-price qualifier, 4s AA Wed / A&BB Tue qualify, Sunday A/BB rotating pairs no-qual, 3-season commitment Oct–April, August mixers, Boomtown Showdown bid) + FAQ/JSON-LD updated; Drop-In House Rules block added (KotC men's-height court, 2-on/2-off, women's net nightly, replay protests, games to 25, stamping/entry, ball-theft ban, bag search, no outside food, under-14 w/ parent, spectators pay); Training hero shares Co-Ed photo + #adult/#youth anchors; Queens Club gets a photo beside the gate, Enter de-looped to women's #qualify, and a schedule line (7:15–9:15 PM · 6 games/night · no late games). **Flag:** hero uses object-fit:cover+center; switch to contain for the entire matrix (black-blended bars) on request.
- **0.6.0 (2026-06-20)** — Home + global pass. **Fixed deploy-blocking bug:** all internal links/assets converted from root-absolute to **relative** paths across all 10 pages (pages now load on the project sub-path / locally; was 404ing every nav + image). Favicon added site-wide = gold spiker mark (cut from 2026 BOOM logo, bg removed); `boom-logo.png` transparent canonical asset added. Home hero bg → new cropped action collage (bottom row removed, top-anchored). Cinematic Tournaments: removed eyebrow + "One-day brackets" + "USAV·AVP·AAU sanctioned"; now **No Membership Fees to Play · More Games, Better Prizes** (gold/white). Cinematic Drop-In: 1-line title, subhead **New? Come Play!**, hours moved to white body line; pills → $10 +fees · Ages 14+ · All levels welcomed. `spike.jpg` reframed to drop the "Colorado Boom" overlay box. Sanctioned band → **OFFICIALLY SANCTIONED BY**. Partner logos hosted locally (lululemon, Shoot360, Team Evo, Nuggets, CAF[dark tile]); added Community: Chance Sports, My Spark Denver, Denver Nuggets. **Motion system (emil-reviewed):** custom ease curves, hero stagger-rise, scroll-reveal (IntersectionObserver, opt-in so reduced-motion/no-JS = full content), button :active press, CTA shine sweep; all gated by prefers-reduced-motion + hover media queries.
- **0.4.0 (2026-06-20)** — Nav: Leagues dropdown (Women's/Men's/Co-Ed), removed Play-tonight button, Training label. Home: shiny gold-gradient CTA (black/white text), tournaments "Denver Metro's" + spike photo + no "open divisions", drop-in listed as text (no pills), "Want to play everyday?" subhead, card headers-over-subheaders with glowing differentiated gold, fuller mission + About anchor + team photo, "Follow us @boomtownvb / don't miss out", partners intro reworded + logos with names + centered + Home-venue tier removed, new Boomtown FieldhouseUSA section, mailing copy = monthly events (no "alerts"), city SEO. New /facility-rules page (summarized from BT events page). Tournaments → "Volleyball Tournaments" H1, anchor chips, Formats & Rules (Revco 4s, gendered 6s, 4s, short-court doubles) + USAV/AVP links, USAV Qualifier section. Women's: Indoor/Grass programs + indoor qualifying. SEO refreshed across pages.
- **0.3.0 (2026-06-20)** — Top banner ("Official Volleyball Company · Sanctioned by USAV·AVP·AAU"); hero motto "Play Everyday" (Everyday white) + official-company subhead; subheads moved under titles, white/bold, site-wide. Partners only on Home with new tiers (added AAU, Co-Ed League Partners label, expanded Gear, Facility partners, Town Center community, Boomtown FieldhouseUSA). Tournaments page: traditional venue block, Sanctioned-by strip only, Day column added, Island/sand removed. Drop-In FAQ imported from BT facility FAQ. Training renamed "Volleyball Training", sand removed, Colorado Boom club section (placeholder). Contact: Lost&Found first. IG now 3×3 + Load more. Queens Club: shine-gradient gold logo, centered stacked code gate, embossed "Boomtown Athletics", links to queensclubvb.com→womens-league. Hero images on every page (~20% black gradient).
- **0.2.0 (2026-06-20)** — Full multi-page demo: all 7 nav pages built (Tournaments, Drop-In, Women's, Men's, Co-Ed, Training, Contact) + Home + Queens Club. Simplified to two fonts (Anton/Archivo); palette locked to yellow titles / gold details / white body, no gray, no mono. Real copy + photos pulled from boomtownvb.com; drop-in (Thu–Mon, 7–11PM, $10+fees) featured site-wide; Tournaments prioritized first. Partner wall with logos incl. Boomtown FieldhouseUSA. IG scroller. Facility hours removed (vary by programming).
- **0.1.0 (2026-06-20)** — Initialized. Brand tokens from 2026 BOOM + Queens Club logos; theme.css; Home page; Queens Club QR landing; IG feed component (placeholder); robots/sitemap with QC excluded. Remaining pages, Sheet wiring, IG Worker deploy, and official partner logos pending.
